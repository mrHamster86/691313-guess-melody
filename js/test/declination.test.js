// import {assert} from 'chai';
// import {declination} from '../util';

// const MINUTS = [`минута`, `минуты`, `минут`];

// describe(`Склонение числительных`, () => {
//   it(`Передаем в функцию значения 1, 21, 101, ожидаем результат минута`, () => {
//     assert.equal(declination(1, MINUTS), `минута`);
//     assert.equal(declination(21, MINUTS), `минута`);
//     assert.equal(declination(101, MINUTS), `минута`);
//   });
//   it(`Передаем в функцию значения 3, 23, 103, ожидаем результат минуты`, () => {
//     assert.equal(declination(3, MINUTS), `минуты`);
//     assert.equal(declination(23, MINUTS), `минуты`);
//     assert.equal(declination(103, MINUTS), `минуты`);
//   });
//   it(`Передаем в функцию значения 5, 11, 25, 105, ожидаем результат минут`, () => {
//     assert.equal(declination(5, MINUTS), `минут`);
//     assert.equal(declination(11, MINUTS), `минут`);
//     assert.equal(declination(25, MINUTS), `минут`);
//     assert.equal(declination(105, MINUTS), `минут`);
//   });
// });
